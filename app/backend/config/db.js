module.exports = {
    url: "mongodb://localhost/library"
}

// Note: If the local connection fails then 
// try using 127.0.0.1 instead of localhost. 
// Sometimes issues may arise when the local 
// hostname has been changed.